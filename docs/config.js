window.LLM_XRAY_CONFIG = {
  inspectionUrl: './data/inspection.json',
  appTitle: 'LLM-Xray',
  subtitle: 'Traces a real forward pass through a local Hugging Face model — every tokenization step, tensor, and attention operation, captured as it runs.'
};
